id: countries
label: 'Countries'
migration_group: pezbase
source:
  plugin: countries
  key: migrate
process:
  name: country_name
  vid:
    plugin: default_value
    default_value: stem_countries
destination:
  plugin: 'entity:taxonomy_term'
dependencies:
  module:
    - pezbase_import
  enforced:
    module:
      - pezbase_import
