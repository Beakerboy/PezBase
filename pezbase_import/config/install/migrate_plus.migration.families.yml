id: families
label: 'Families'
migration_group: pezbase
source:
  plugin: families
  key: migrate
process:
  name: name
  parent: parent
  vid:
    plugin: default_value
    default_value: dispenser_family
destination:
  plugin: 'entity:taxonomy_term'
dependencies:
  module:
    - pezbase_import
  enforced:
    module:
      - pezbase_import
